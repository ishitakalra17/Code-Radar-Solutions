#include<stdio.h>
int main()
{
    int a,i,j;
    scanf("%d",&a);
    for(i=0;i<n;i++)
    {
        if(i==0 ||j i==n)
        {for(j=0;j<n;j++)
        {printf("*");}}
        else if(j==1 || j==n )
        {for(j=0;j<n;j++)
        {printf("* ");}}
        printf("\n");
    }
    return 0;
}